{
    "name": "@ekd-desk/platform",
    "version": "1.0.0",
    "description": "Platform-specific implementations for EKD Desk",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "test": "jest",
        "lint": "eslint src --ext .ts",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@ekd-desk/shared": "file:../shared"
    },
    "devDependencies": {
        "@types/jest": "^29.5.8",
        "typescript": "^5.3.0",
        "jest": "^29.7.0",
        "rimraf": "^5.0.5"
    }
}